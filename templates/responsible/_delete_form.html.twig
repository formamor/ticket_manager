<form method="post" action="{{ path('app_responsible_delete', {'id': responsible.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ responsible.id) }}">
    <button class="btn">Delete</button>
</form>
